@{
    ViewData["Title"] = "Check Result";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }
<div class="container-fluid bg-light">
    <div class="row">
        <h3 class="text-uppercase text-center text-info mt-5 mb-5" style="font-weight:bold">Check Result Form</h3>
    </div>
    <div class="row">
        <div class="col-lg-4 col-md-6 col-sm-12">
            <div class="d-flex">
            <input type="text" placeholder="Roll No" class="form-control mb-5" id="StudentId" />
                <a class="ms-3" onclick="CheckResultbyId();"><i class="fa fa-search" style="font-size:36px;color:red"></i></a>
            </div>
        </div>
        <div class="col-lg-8 col-md-8 col-sm-12">
            <select id="ClassIds" class="form-select w-25" onchange="GetSectionNames();"></select>
            <div class="mt-2">
                <form id="SectionName" class="d-flex" action="/action_page.php"></form>
            </div>
        </div>
        <div class="col-lg-4 col-md-6 col-sm-12"></div>
        <div class="col-lg-4 col-md-6 col-sm-12"></div>
    </div>
    <div class="row">
@*        <div class="col-lg-12 col-md-12 col-sm-12">
            <div class="table-responsive" data-pattern="priority-columns" data-sortable="true">
*@          <table class="table table-striped table-hover table-small-font table-tighten table-bordered" id="example">
                <thead>
                    <tr id="tableheader">
                    </tr>
                </thead>
                <tbody id="tbody" class="border">
                </tbody>
            </table>
            @* </div>
        </div>*@
     </div>
</div>
<script src="~/views/result/checkresult.js"></script>


